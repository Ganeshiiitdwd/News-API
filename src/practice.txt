const router=createbrowserrouter([
    {path:, element:, children:[
        {index:true, element, },
        {path;, element},
        {path:/produxtid/:propid, element:}
    ]}
])
<RouterProvider router={router}/>